# Database configuration
MYSQL_ROOT_PASSWORD=password
MYSQL_DATABASE=database
MYSQL_USER=user
MYSQL_PASSWORD=password

# Backend datasource configuration (for independent or compose operation)
DB_HOST=host
DB_PORT=3306
DB_NAME=name
DB_USER=user
DB_PASSWORD=password

# Frontend API upstream (compose service DNS or external backend URL)
API_UPSTREAM=http://backend:8080